import { Typography } from '@mui/material';

const TaskManager = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Task Manager
      </Typography>
    </div>
  );
};

export default TaskManager;